version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .
    files:
      - index.html
      - dist/**/*
  cache:
    paths:
      - node_modules/**/*
